{
  "servers": {
    "minecode-debug": {
      "type": "stdio",
      "command": "${command:python.interpreterPath}",
      "args": ["-m", "minecode.server"],
      "cwd": "${workspaceFolder}"
    }
  },
  "inputs": []
}
